export interface Catch {
  catchId: number;
  userId: number;
  fishName: string;
  fishWeight: number;
  size: number;
  photoUrl: string;
  catchDate: Date;
  kept: boolean;
  spotPublic: boolean;
  createdAt: Date;
}
